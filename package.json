{
  "name": "athro-federation",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "build:dashboard": "turbo build --filter=athro-dashboard...",
    "build:dashboard-explicit": "turbo build --filter=@athro/shared-types --filter=@athro/shared-services --filter=@athro/shared-ui --filter=@athro/shared-athros --filter=athro-dashboard",
    "build:workspace": "npm run build --workspace=@athro/shared-services && npm run build --workspace=@athro/shared-ui && npm run build --workspace=@athro/shared-types && npm run build --workspace=@athro/shared-athros && npm run build --workspace=athro-workspace-2",
    "build:dashboard-complete": "turbo build --filter=@athro/shared-types --filter=@athro/shared-services --filter=@athro/shared-ui --filter=@athro/shared-athros --filter=athro-workspace-2 --filter=athro-dashboard"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/moment": "^2.11.29",
    "@types/react-big-calendar": "^1.16.2",
    "moment": "^2.30.1",
    "react-big-calendar": "^1.19.2",
    "react-icons": "^5.5.0"
  }
}
