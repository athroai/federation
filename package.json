{
  "name": "athro-federation",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "build:dashboard": "cd apps/athro-dashboard && npm install && npm run build",
    "build:workspace": "cd apps/athro-workspace-2 && npm install && npm run build"
  },
  "devDependencies": {
    "turbo": "^1.10.0"
  },
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "@types/moment": "^2.11.29",
    "@types/react-big-calendar": "^1.16.2",
    "moment": "^2.30.1",
    "react-big-calendar": "^1.19.2",
    "react-icons": "^5.5.0"
  }
}
